#include <stdio.h>

int main()
{
    int a, b, max;
    printf("Enter two numbers : ");
    scanf("%d %d" , &a, &b);
    // using conditinal operator
    max = (a > b) ? a : b;
    printf("Maximum nuber is : %d\n",max);

    return 0;
}
